<!-- フォローしているかどうかでfollow,unfollowのhtmlに飛ぶ -->
<!-- User一覧用に -->
<% if user_signed_in? %>

	<div id="follow_form">
		<% if current_user.following?(user) %>
		<%= render 'unfollow', user: user %>
	<% else %>
		<%= render 'follow', user: user %>
	<% end %>
	</div>
<% end %>
